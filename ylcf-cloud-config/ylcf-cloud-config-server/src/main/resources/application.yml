spring.application.name: config-server
server.port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaoDongMr/spring-could-demo
          searchPaths: config/app
          username: xiaoDongMr
          password: dongdong456852
      label: master