server.port: 8999

spring:
  application:
    name: config-client
  profiles:
    active: dev
  cloud:
    config:
      label: master
      uri: http://localhost:8888
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://root:root@localhost:8761/eureka


      #dev开发环境配置文件
      #test测试环境
      #pro正式环境